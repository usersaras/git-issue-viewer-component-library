export declare const sizeVariants: {
    xs: {
        padding: string;
        fontSize: string;
    };
    sm: {
        padding: string;
        fontSize: string;
    };
    base: {
        padding: string;
        fontSize: string;
    };
    lg: {
        padding: string;
        fontSize: string;
    };
    xl: {
        padding: string;
        fontSize: string;
    };
};
export type SizeVariantsPropType = keyof typeof sizeVariants;
